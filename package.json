{
  "name": "chikage",
  "version": "0.0.1",
  "description": "the chikage server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "/usr/bin/env lsc -bc server.ls",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "caasi Huang",
  "license": "GPLv3",
  "dependencies": {
    "express": "^4.12.3",
    "redis": "^0.12.1"
  }
}
